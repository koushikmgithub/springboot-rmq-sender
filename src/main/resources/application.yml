server:
  port: 9095
spring:
  application:
    name: springboot-rmq-sender
  rabbitmq:
    host: rabbitmq-keda-cluster.default.svc.cluster.local
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"      
